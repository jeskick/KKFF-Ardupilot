# 提交最简流程（代码改动 + 编译产物）

```bash
cd /home/kk/FFardupilot
/usr/bin/git checkout master && /usr/bin/git pull
/usr/bin/git checkout -b feat/<name>

export PATH=/opt/gcc-arm-none-eabi-10-2020-q4-major/bin:/usr/lib/ccache:$PATH
./waf configure --board MatekH743 && ./waf plane
./waf configure --board MatekF405-Wing && ./waf plane

/usr/bin/git add <你的代码文件> <你的文档文件>
/usr/bin/git add -f build/MatekH743/bin/*with_bl*.hex build/MatekH743/bin/arduplane.apj build/MatekH743/bin/arduplane.bin
/usr/bin/git add -f build/MatekF405-Wing/bin/*with_bl*.hex build/MatekF405-Wing/bin/arduplane.apj build/MatekF405-Wing/bin/arduplane.bin
/usr/bin/git status --short && /usr/bin/git diff --cached --name-only
/usr/bin/git commit -m "本次功能说明（含代码与产物）"
```

详细规范见：`docs/next_submit_checklist_cn.md`
